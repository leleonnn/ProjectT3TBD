export const metadata = {
    title: "Publisher - Good Reading Bookstore",
  };
  
  const PublisherLayout = ({ children }: { children: React.ReactNode }) => {
    return <div>{children}</div>;
  };
  
  export default PublisherLayout;