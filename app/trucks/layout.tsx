export const metadata = {
    title: "Truck - Good Reading Bookstore",
  };
  
  const TruckLayout = ({ children }: { children: React.ReactNode }) => {
    return <div>{children}</div>;
  };
  
  export default TruckLayout;