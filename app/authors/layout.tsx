export const metadata = {
    title: "Author - Good Reading Bookstore",
  };
  
  const BookLayout = ({ children }: { children: React.ReactNode }) => {
    return <div>{children}</div>;
  };
  
  export default BookLayout;