export const metadata = {
    title: "Shelf - Good Reading Bookstore",
  };
  
  const ShelfLayout = ({ children }: { children: React.ReactNode }) => {
    return <div>{children}</div>;
  };
  
  export default ShelfLayout;