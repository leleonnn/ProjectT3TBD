export const metadata = {
    title: "Book - Good Reading Bookstore",
  };
  
  const BookLayout = ({ children }: { children: React.ReactNode }) => {
    return <div>{children}</div>;
  };
  
  export default BookLayout;